export function trackProductClick(productId) {
  console.log(`Product clicked: ${productId}`);
  // Mocked: Send to analytics service
}

export function trackSearch(query) {
  console.log(`Search performed: ${query}`);
  // Mocked: Send to analytics service
}
